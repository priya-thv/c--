#include<iostream>
using namespace std;

struct node{
   int data;
   node*next;
};

node*createnode(int data){
    node*newnode=new node();
    newnode->data=data;
    return newnode;
}

int main(){
    int n;
    cin>>n;
    node*head=NULL;
    node*temp=NULL;
    for(int i=0;i<)
}